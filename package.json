{
  "name": "minishake",
  "version": "0.0.1",
  "description": "a build system",
  "license": "CC0-1.0",
  "author": "",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "shk": "./dist/cli.js"
  },
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "bootstrap": "node bootstrap.js",
    "build": "node dist/cli.js build ts-compile",
    "verify": "node bootstrap.js && cp -r dist dist-stage0 && rm -rf .shk build/ts dist && node bootstrap.js && node dist/cli.js build ts-compile && diff -r dist-stage0 dist && rm -rf dist-stage0 && echo 'Stage 2: PASS — bootstrap is reproducible'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "t2lang": "file:../t2lang"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
